{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Certificate DEX",
  "description": "Certificate content for verification of trading on a decentralized exchange.",
  "type": "object",
  "properties": {
    "address": {
      "type": "string",
      "format": "ethereum-address",
      "description": "The Ethereum address of the decentralized exchange."
    },
    "totalSwapVolume": {
      "type": "string",
      "format": "decimal",
      "description": "The total volume in USD equivalent traded by the user on the exchange."
    },
    "swapVolumeYear": {
      "type": "string",
      "format": "decimal",
      "description": "The volume in USD equivalent traded by the user on the exchange over the past year.",
      "default": "0"
    },
    "swapVolumeHalfYear": {
      "type": "string",
      "format": "decimal",
      "description": "The volume in USD equivalent traded by the user on the exchange over the past half year (6 months).",
      "default": "0"
    }
  },
  "required": [
    "address",
    "totalSwapVolume"
  ],
  "additionalProperties": false
}
