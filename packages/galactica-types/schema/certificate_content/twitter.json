{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Twitter Certificate Content",
  "description": "Certificate content for X/Twitter account verification data.",
  "type": "object",
  "properties": {
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp when the account was created. Must be less than the current time."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_]+$",
      "description": "The ID of the Twitter account, interpreted as string to be hashed."
    },
    "followersCount": {
      "type": "integer",
      "minimum": 0,
      "description": "The number of followers the account has.",
      "default": 0
    },
    "followingCount": {
      "type": "integer",
      "minimum": 0,
      "description": "The number of accounts the user is following.",
      "default": 0
    },
    "listedCount": {
      "type": "integer",
      "minimum": 0,
      "description": "The number of lists the account is a member of.",
      "default": 0
    },
    "tweetCount": {
      "type": "integer",
      "minimum": 0,
      "description": "The number of tweets the account has posted.",
      "default": 0
    },
    "username": {
      "type": "string",
      "format": "case-insensitive",
      "minLength": 4,
      "maxLength": 15,
      "description": "The username of the Twitter account, it is case-insensitive, meaning that 'AStar_Gala' and 'astar_gala' lead to the same account and zk field element."
    },
    "verified": {
      "type": "boolean",
      "description": "Whether the account is verified.",
      "default": false
    }
  },
  "required": [
    "createdAt",
    "id",
    "username"
  ],
  "additionalProperties": false
}
